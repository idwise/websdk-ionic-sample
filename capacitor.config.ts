import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'idwise-ionic-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
